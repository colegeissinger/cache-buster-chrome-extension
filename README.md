# cache-buster-chrome-extension
A Google Chrome extension that can append a unique query string to your browsers address bar and refresh the page. This is to help bypass server side caches like Akamai.
